// Aula

// JavaScript tem uma tipagem dinâmica
// Untyped

let minhaVar = 567;
minhaVar = "texto";
minhaVar = true;

// É possível atribuir outro tipo de dado para a mesma variável
// Tipagem dinâmica é o JS inferir qual é o tipo do dado dinamicamente


// JavaScript é Multiparadigma

// JavaScript = ECMAscipt

// ES6 / ES2015
    // Verão do JS que saiu em 2015 que a partir dessa versão passou a ter versões anuais e a versão que trouxe várias funcinalidades importantes

// JavaScript é uma linguagem interpretada
    // Existem dois tipos de linguagens, as interpretadas e as compiladas.
    // A linguagem interpretada é interpretada na hora em que é executada. 
    // A linguagem compilatada é compilada para uma linguagem de máquina e quando é executada, é executada a linguagem da máquina.